<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.animesocialapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimeSocialApp">
        <activity android:name=".animeManagment.AnimeDetailActivity"></activity>
        <activity android:name=".listManagment.ListDetailActivity" />
        <activity android:name=".listManagment.AddAnimeActivity" />
        <activity android:name=".listManagment.CreateListActivity" />
        <activity android:name=".reviewManagement.PostReviewActivity" />
        <activity android:name=".mainManagement.MainActivity" />
        <activity android:name=".loginManagement.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>